import {notFound} from "next/navigation"; import {projects} from "@/lib/data"; import Link from "next/link"; import {ArrowLeft,ArrowUpRight} from "lucide-react"
export function generateStaticParams(){return projects.map(p=>({slug:p.slug}))}
export default async function CaseStudy({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=projects.find(x=>x.slug===slug);if(!p)return notFound();return <section className="container py-36"><Link href="/portfolio" className="eyebrow inline-flex items-center gap-2"><ArrowLeft size={12}/> Back to work</Link><div className="mt-12 grid gap-14 md:grid-cols-[1.5fr_1fr]"><div><div className="eyebrow">{p.tag} / {p.category}</div><h1 className="display mt-6">{p.title}</h1><p className="mt-8 max-w-2xl text-lg leading-8 muted">{p.description}</p></div><div className="grid-noise panel aspect-square flex items-end p-7"><div className="eyebrow">VAYREN DIGITAL / {p.number}</div></div></div><div className="mt-24 grid gap-8 border-t border-[#242424] pt-10 md:grid-cols-3"><div><div className="eyebrow">Challenge</div><p className="mt-4 leading-7">{p.challenge}</p></div><div><div className="eyebrow">Strategy</div><p className="mt-4 leading-7">{p.strategy}</p></div><div><div className="eyebrow">Technology</div><div className="mt-4 flex flex-wrap gap-2">{p.tech.map(t=><span className="border border-[#333] px-3 py-2 text-xs" key={t}>{t}</span>)}</div></div></div><div className="mt-24"><div className="eyebrow">Deliverables</div><div className="mt-6 grid gap-px bg-[#242424] md:grid-cols-2">{p.deliverables.map((x,i)=><div className="bg-black p-5 text-sm" key={x}><span className="mr-5 text-neutral-600">0{i+1}</span>{x}</div>)}</div></div><div className="mt-20 border-t border-[#242424] pt-10"><p className="eyebrow">No fabricated metrics</p><p className="mt-4 max-w-2xl muted">This showcase intentionally avoids unverified revenue, sales, conversion rates, awards, testimonials or client claims.</p></div><Link href="/contact" className="btn btn-solid mt-10">START A PROJECT <ArrowUpRight size={14}/></Link></section>}
